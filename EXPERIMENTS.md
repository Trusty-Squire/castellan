# EXPERIMENTS.md — ad-hoc experiment log

Index of experiments run against ser (beyond the formal `scripts/experiment.ts`
benchmark in RESULTS.md). One row per experiment: the question, the finding, and
where the raw data/scripts live. **Conclusions** that matter long-term also go to
the cross-session memory (`…/memory/`) and to commit messages; this file is the
*index* so a run can be revisited, not re-derived from memory.

Data dirs live under `/home/lunchbox/` (demo artifacts, deliberately NOT in the repo
to avoid bloat). If a dir is cleaned up, its row's finding still stands via memory.

| date | experiment | question | finding (1-line) | data / scripts |
|---|---|---|---|---|
| 2026-06-20 | **gypsy full funnel** (codex backend) | does ser build a real app e2e from a vague, ambition-laden prompt? | built a working, audit-clean app — but the visual judge **false-passed** themed text as "immersive"; kicked off the whole judge investigation | `gypsy-build/` |
| 2026-06-20 | **arcade probe** (20 one-shot games + eval) | strip popular games so the model can't rote-implement — does it then fail? | cheap model one-shots *plausible* versions of even obscure games; binding constraint is **verification, not capability/memorization**; the judge is the bottleneck (3rd time) | `arcade-probe/` (`eval-results.json`) |
| 2026-06-20 | **spec-probe** (unstrip: constrained vs unconstrained extractIdea) | does the model under-build, or does the harness strip it? | model proposes **2–3.5× richer** shape unconstrained; ser's "recommend the smaller build" prompt strips it to a gateable skeleton → drove the **unstrip** commit | `spec-probe/` (`*.json`, `before/`) |
| 2026-06-20 | **holdem `--to spec` ×7** (dom-behavior selection) | will the planner SELECT a browser/DOM gate for UI? | **no, 4×** — anchored on curl/grep/vitest; found + fixed a footgun (browser absent from `detectAvailableTools`); resolved by **harness-attaching** the gate (commit `0007efe`) | `holdem-spec2…7/` (`mission.yaml`) |
| 2026-06-20 | **arcade iteration** (gated loop, VLM judge) | does a cheap loop lift the floor? | **8 wins** (broken→playable) but the noisy VLM gate caused **2 regressions + 2 stalls**; gate reliability bounds loop quality | `arcade-iterated/` (`iterate-results.json`) |
| 2026-06-20 | **rubric build-loop** (habit tracker, deterministic checks) | web rubric → component contract → deterministic dom-gate, no VLM? | cheap model **one-shot 6/6** components against the contract; verified by dom-gate, **zero VLM calls** | `rubric-demo/` (`result.json`, `build-loop.mjs`) |
| 2026-06-20 | **rubric VLM polish loop** (opus, then gpt-5.5/codex) | does the VLM add value the presence-check can't see? | **yes** — it caught an **empty chart that passed the presence check** (present≠complete); adversarial+pairwise held **cross-model**; but a holistic "more polished?" ratchet **won't force a specific defect closed**, and the cheap builder hit a **ceiling** (couldn't fix the chart in 3 tries) | `rubric-demo/` (`build-loop-vlm.mjs`, `vlm_*.png`, `vlm-*.log`) |
| 2026-06-21 | **ser replicates trustysquire.ai** (falsification, spec phase, cheap chain) | can the funnel rebuild the owner's flagship — esp. its SOUL (never return the raw secret; inject/leash)? | builds the SHAPE (7-req decomposition: bot, extraction, AES-at-rest, auth w/ 401, agent API, handlers, UI) but INVERTS the soul — R5 gates `GET /api/credentials/user | grep api_key` (the raw-key-on-read ANTI-PATTERN, hardened into a passing gate) + crypto derives the key from the user password to serve it. Ambiguity machinery fired (3 forks) but only on IMPLEMENTATION details (auth method, CAPTCHA, crypto algo) — never the load-bearing expose-vs-leash architecture. Garbage-vision-in (prompt asked for raw read) → coherently-gated-garbage-out. Roadmap: should the eng/security reviewer flag credential-exposure as a user_challenge fork? | `squire-clone/` (`squire.spec.yaml`, `spec.log`) |
| 2026-06-21 | **ser BUILDS trustysquire** (full cheap funnel, mock providers) | does the safer design survive the BUILD; do this session's gates fire; ship or honest-halt? | **HONEST-HALT** (jidoka, on a real cheap build): bot_client PASS (signup→key vs mock), secure_storage PASS (AES) — but auth_module FAIL ×4 → HALT. ROOT CAUSE = a HARNESS gate-authoring gap, NOT model incapacity: the planner wrote server-dependent HTTP gates (`curl localhost:8000/login …`) with NO server-boot step → un-passable by construction. Same class #1 fixed for the DOM gate (`--serve`), now needed for HTTP/API command gates. The #1 DOM gate DID attach correctly to web_ui (login-form/keys-list/api-key/logout). Dispute mechanism did NOT fire (model retried an un-passable gate 4× instead of disputing). Soul: this run's spec surfaced 4 security forks but the api gate still compiled raw-read (`/api/keys/vouchflow → api_key`) — expose-vs-leash fix is run-to-run unreliable; build halted before we could see if leashed design survives. | `squire-build/` (`build.log`, `mission.yaml`, `.squire/trace-*.jsonl`) |
| 2026-06-21 | **serve-gate + re-run** (fix the HTTP-gate halt, rebuild) | does booting the server unblock auth-api; what's the NEXT wall? | serve-gate SHIPPED (boot/wait/check/teardown + start-inference): re-derive wraps the HTTP gates; runtime failure moved exit 4 (curl dead port) → exit 3 (tried to boot). Re-run still HALTS on auth-api — peeled to the REAL causes: (1) my inferStartCommands scanned only the root, but the node's blast_radius forced the server into `src/` → "no server file" (FIXED: scan src/ + 1 level); (2) env lacks fastapi/uvicorn/flask → a framework server can't boot under zero-network; (3) blast_radius `[src/auth-api.js,+test]` too narrow to write a package.json/entrypoint (deny ×3). Honest-halt held at every layer (never false-shipped). Remaining = greenfield-provisioning design forks (stdlib-nudge vs provision deps; widen radius). | `squire-build2/` (`build.log`, mission, trace) |
| 2026-06-21 | **contract-coherence fix + re-run** (the auth wall) | can the harness boot a contract-pinned testable factory for the live gate? | YES — root cause was CONTRACT/GATE INCOHERENCE (contract pins `createApp()` factory w/ no listen(); gate needs a live server). Fixed harness-owned: serve-gate FACTORY-BOOT candidate (import+resolve+listen) + waitForPort abandons a candidate when its process exits + brief addendum telling the node to self-listen (Node/Flask/FastAPI). Re-run: exit code marched **4→3→1** across the session — exit 1 = server BOOTED, an endpoint was wrong. Harness no longer the boot bottleneck; binding constraint moved to the cheap model's ENDPOINT CORRECTNESS. Honest-halt held throughout. Deferred: Python harness-owned factory-boot (Node only). | `squire-build4/` (`build.log`, mission) |
| 2026-06-21 | **"delegated wrongly" root-cause + fix** (owner pushback: qwen is powerful, it's a harness failure) | why is auth_module unsatisfiable; is it really the model? | NOT the model — proven UNSATISFIABLE by any model (a correct secure server also fails: logs in as a testuser nobody seeds). Root = AUTHORING delegated to the cheap model: the IDEA phase (which writes the acceptance GATES) ran on chain.executor → unsatisfiable gate, then (once self-seed nudged) TAUTOLOGICAL gates (echoes its own flask app). FIX: route idea→chain.knight (decompose/infer already were) + SELF-CONTAINED-GATE rule (seed via the BUILD's interface, never assume state, never echo a throwaway server) + decompose COHERENCE_RULE (integration reqs → a node that builds+deps+seeds the flow, never a library node). VALIDATED: knight now authors satisfiable, self-seeding gates (register via the built endpoint THEN login; no tautologies) and a coherent decomposition (api-server = proper integration node: builds server.js, deps on auth-storage+bot-module). Residual: bot-module gate reaches :3000 without dep on api-server (2nd-order). | `squire-c6/` (spec, mission) |
| 2026-06-21 | **gate satisfiability proofreader** (the deterministic fix) | can the harness catch its own unsatisfiable gates instead of relying on the author? | YES, built + validated. After prompt fixes still leaked unseeded gates (even knight-authored), added a PURE high-precision detector (gateProofread: flags a SELECT…WHERE or login/-u auth with NO seeding mechanism) + a planner repair (re-author to seed via the build's interface) wired into derive (pass 3d). Validated on the EXACT crypto_storage gate that halted the build: BEFORE = flagged; repaired → `storage.create_user(...)+update_user_keys(...) && SELECT…` (seeds first), AFTER = clean. +7 hermetic tests (real halting gates as fixtures). The harness now proofreads its own checks before the build burns the ladder. | `gate-proofread.ts`, `squire-c8/` | 
| 2026-06-21 | **port-coherence guard + clean build run** (finish the leftovers) | does a fully-coherent mission build, or halt for the right reason? | portCoherence guard added (a node whose serve-gate boots a server it doesn't build → deterministically depend on the node that builds it; the bot-module→web-app fix). Re-derive → FULLY COHERENT mission (0 unsatisfiable gates; dep auto-added). Build ran + halted at crypto-storage gate exit **1** (ran, check failed). Halt looked like model-capability but skeptical re-investigation (owner) found a THIRD coherence gap: the node wasn't EQUIPPED — gate writes SQLite from Node but radius had no package.json to install a driver; qwen fixated on npm install, never wrote a working storage.js. Built the TOOLING-COHERENCE guard (toolingCoherence: add referenced scripts + a manifest to the node's radius). Re-derive: every node equipped (still-missing=[]). 338 tests. THREE guards now: seeding, port, tooling. | `gate-proofread.ts`, `squire-c9/` |
| 2026-06-21 | **safety-fork fix** (IDEA prompt, re-run same prompt) | can ser be made to CHALLENGE a stated security anti-pattern instead of gating it? | YES — added a SAFETY fork to IDEA_PROMPT (mirrors the existing LEGITIMACY clause; carved out of the "settled fact" rule; scoped to sensitive-asset products). Re-run surfaced a NEW bucket-1 ask: "Security approach for handling sensitive API credentials → use a vault with scoped access tokens instead of raw keys", AND independently raised the ToS/legitimacy fork. Propagated into the spec: the raw-`grep api_key`-over-HTTP requirement became a vault/encryption-at-rest/scoped-token posture. Generalized, not hardcoded; 308/308. | `squire-clone2/` vs `squire-clone/` |
| 2026-06-21 | **closure-judge replay** (empty-chart screenshots vs `reviewClosure`) | does the adversarial closure judge refuse the false-pass the holistic ratchet gave? | **yes** — held the empty chart `present` across BOTH before/after shots (old loop said "✓ polished, kept" ×3 and would have shipped it), abstained `unsure` when it couldn't prove a fix, and confirmed a genuine tile fix — frozen-list + per-defect + abstain all working on the real case | `rubric-demo/closure-check.mjs` (shots: `vlm_FINAL_*.png`) |
| 2026-06-28 | **SWE-bench Requests 6/6** (automated 6028 repair) | can the Requests slice hit 6/6 without the manual 6028 diagnostic patch? | **yes** — oracle/repair prompt fixes + constrained whole-function SEARCH fallback recover the minimal `prepend_scheme_if_needed` auth/netloc fix; official single 6028 `1/1`, official Requests slice `6/6`; next bigger slice should switch to pytest with `k=3,r=0,oracle-repair=1,repair-rung=2`, not rerun solved Requests | `projects/swebench/EXPERIMENTS.md`, `predictions-requests-six-auto.jsonl`, `ser-select-v2.requests-six-auto.json` |
| 2026-06-28 | **SWE-bench pytest k=3 oracle-repair** | does the Requests-proven oracle/repair config scale to the pytest six slice? | **6/6 official after focused 5787 regression repair** — first pass was 5/6 because 5787 fixed F2P but regressed `test_deserialization_failure`; focused repair preserved that P2P path and the combined v2 pytest slice resolved all six | `projects/swebench/predictions-pytest-six-k3-oracle-repair-v2.jsonl`, `ser-select-v2.pytest-six-k3-oracle-repair-v2.json` |
| 2026-06-28 | **SWE-bench local 14 bounded** (Requests 8 + pytest 6) | can the remaining Requests hang be handled by the leanest harness mutation instead of a source workaround? | **13 official + 1 bounded = 14/14 local** — Requests seven official `7/7`, pytest six official `6/6`, and 2317 passes all F2P plus 132/132 bounded P2P when the external-network `test_connection_error` infra node is excluded; recorded as bounded, not official | `projects/swebench/ser-select-v2.local-14-bounded.json`, `ser-select-v2.requests-2317-bounded.json`, `predictions-local-14-bounded.jsonl` |
| 2026-06-28 | **SWE-bench Django six** (native runner + context repairs) | does the harness generalize to Django after Requests/Pytest saturation? | **6/6 official** — added Django native `tests/runtests.py` node execution, robust SEARCH parsing, literal-bearing class context, media-ordering helper context; qwen selected 4, focused repair closed FilePathField/media toposort | `projects/swebench/predictions-django-six-native-repair-v2.jsonl`, `ser-select-v2.django-six-native-repair-v2.json`, `ser-select-v2.local-20-bounded.json` |
| 2026-06-28 | **SWE-bench harness lessons + next serious test** | what general lesson explains the Requests/Pytest/Django score lift, and what should be tested next? | score came from removing harness signal loss before blaming the model: native runners, bounded P2P, exact context, robust patch apply, and focused verifier feedback; next test should be a fresh mixed-repo 24-slice with miss-classification, not another saturated rollup | `projects/swebench/EXPERIMENTS.md` |
| 2026-06-20 | **deep-research ×2** (ambiguity; LLM-as-judge) | how to detect under-determination / build a trustworthy judge? | divergence>self-confidence; gateability as the resolution axis; judge must abstain; VLM judges fail at perception, decompose + adversarial + reference-anchor | memory: `castellan-ambiguity-research`, `castellan-judge-design` |

## Standing practice
When you run an experiment worth remembering: add a row here (question + 1-line
finding + data path), and if the conclusion is durable, also write/update a memory
file. Don't move the raw data into the repo — index it.
| 2026-06-21 | **full build, all 3 guards** (the real verdict) | with seeding+port+tooling guards, is the halt finally the model? | YES, ruled out every harness explanation: gate FAIR (correct async base64+sqlite storage passes, exit 0), node EQUIPPED (package.json present), code RAN (no crash), code WRONG (trace shows qwen stored keys in PLAINTEXT `vouchflow_key_123` ×4, + wasted rungs installing `base64-js` instead of built-in Buffer). Genuine cheap-model-capability halt — harness correctly refused to ship plaintext creds = jidoka. The owner's skepticism drove ruling out 4 harness gaps before this verdict was earned. | `squire-c10/` (build.log, trace) |
